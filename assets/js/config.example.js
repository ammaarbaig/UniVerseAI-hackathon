// Copy to config.local.js and fill in. config.local.js is gitignored.
// WARNING: any key here is visible to every visitor of the deployed site.
// Use only for a private demo. For public launch, put the key behind a proxy.
window.UNIVERSE_CONFIG = { OPENCODE_API_KEY: '' };
